version: '3.8'

services:
  smpark-oauth2.0:
    build:
      context: ../../
      dockerfile: apps/smpark-oauth2.0/Dockerfile
      target: production
    image: smpark9596/smpark-oauth2.0:${OAUTH_VERSION}
    platform: linux/amd64
    container_name: smpark-oauth2.0-${OAUTH_VERSION}
    ports:
      - '3333:5555'
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
    volumes:
      - smpark-log:/usr/src/oauth-log
    command: yarn smpark-oauth2.0:prod

networks:
  default:
    name: smpark-oauth-network-prod

volumes:
  smpark-log:
